package hu.nero.model;

import hu.nero.car.PassengersCar;
import hu.nero.car.TransmissionType;

public class Camry extends PassengersCar {

  public Camry(String color, int maxSpeed, TransmissionType transmissionType, double price) {
    super(color, maxSpeed, transmissionType, price);

  }

}
